server:
    port: 8888

spring:
  application:
    name: config-Server

  profiles:
    active: native
    

  cloud:
    config:
      server:
        git:
          uri: https://github.com/MaheshaDinu/cloud-native-e-library-configurations.git
          search-paths: platform,services
        native:
          search-locations:
            - classpath:/configurations
            - classpath:/configurations/platform
            - classpath:/configurations/services